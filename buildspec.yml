version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - sudo yum update -y
      - sudo npm install

  build:
    commands:
      - echo Build phase completed

artifacts:
  files:
    - '**/*'